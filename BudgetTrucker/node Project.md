## Week 1 Mini Project (Budget Tracker)

- Stored data as array of objects: [{ product, price }]
- addExpense(name, price) adds to expenses
- listExpenses(arr) prints all items
- totalSpending(arr) sums all prices
- mostExpensiveItem(arr) finds highest price (handles empty array)
